import React from "react";

export default function CorrectnessPage(props) {
  let file = props.file;
  console.log("file ", file);
  return (
    <div>
      <p>hello</p>
      {/* {file != null && <div>{file}</div>} */}
    </div>
  );
}
